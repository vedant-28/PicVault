# PicVault
A full-stack web application that allows users to upload and view images in gallery format.
# SELECT size_bytes, uploaded_at, BIN_TO_UUID(id) AS id, content_type, original_filename, storage_key FROM image_metadata;