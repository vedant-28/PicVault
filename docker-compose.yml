services:
  mysql-db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ALLOW_EMPTY_PASSWORD: false
      MYSQL_RNADOM_ROOT_PASSWORD: root
    ports:
      - 3306:3306

  minio:
    image: minio/minio:latest
    container_name: minio-server
    ports:
      - "9000:9000"     # S3 API Port
      - "9001:9001"     # Web Console UI Port
    environment:
      MINIO_ROOT_USER: root
      MINIO_ROOT_PASSWORD: passw0rd1234
    command: server --console-address ":9001" /data
    restart: always
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio-init:
    image: minio/mc:latest
    container_name: picvault-minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 root passw0rd1234 &&
      mc mb --ignore-existing local/picvault-images &&
      echo 'Bucket ready: picvault-images (private — no listing, no anonymous access)'
      "
volumes:
  minio-data:

# backend:
#   environment:
#     MINIO_URL: http://minio:9000
#     MINIO_ACCESS_KEY: root
#     MINIO_SECRET_KEY: passw0rd1234
#     MINIO_BUCKET: picvault-images